Q1:
async was hardest because i had to learn that the code must wait for data to arrive before it can move to the next line

Q2:
When I changed the list type from integers to doubles, I realized I had to update several parts of the code. This really showed me why choosing the right data type at the beginning is so important—it saves a lot of extra work later on

Q3:
When you look at an app, it often waits for the server’s response, so the user sees a loading icon until the answer returns.

Q4:
By using separate methods for each math operation, it’s simpler to identify and fix any mistakes, rather than relying on one large function that handles everything.
